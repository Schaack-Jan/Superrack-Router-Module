node_modules/
package-lock.json
/pkg
/*.tgz
DEBUG-*
/.yarn
.idea
.vscode
.DS_Store
/dist
/build
/.env
.env.local
.env.*.local
coverage/

# Graphify architecture graph: only the final graph + report are tracked
.graphify/*
!.graphify/graph.json
!.graphify/GRAPH_REPORT.md
# stray nested caches created by the graphify git hook
*/**/.graphify/

# Windows MIDI helper build output (built in CI / locally, not tracked)
helper/packages/
helper/dist/
helper/**/bin/
helper/**/obj/